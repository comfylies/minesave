# 游戏存档分享平台 (GameSaving) — 项目任务书

> 更新日期: 2026-06-19 | 原始版本: 2026-06-18

---

## 一、核心产品定位

- **像 GitHub 一样浏览**：清晰的文件目录列表 + 面包屑导航
- **像知乎/CSDN 一样阅读**：图文 Markdown 渲染 + 代码高亮
- **像 Word 一样协作**：README 选中批注 + 侧边栏审阅

三栏式布局：顶部导航 | 中间（文件列表 + README）| 右侧批注侧边栏

---

## 二、技术栈

| 层 | 技术 |
|----|------|
| 后端 | Spring Boot 3.5 + Java 17 + Spring Data JPA + HikariCP |
| 数据库 | MySQL 8.0 (InnoDB, utf8mb4) |
| 前端 | Vue 3 + Vite + Element Plus |
| 核心库 | Apache Commons Compress + BCrypt + highlight.js |
| ZIP 解析 | Apache Commons Compress（UTF-8/GBK 回退） |

---

## 三、数据库设计 (gamesaving)

### ER 关系

```
game    1──N article         一款游戏 ← 多个存档文章
user    1──N article         一个用户 ← 多个存档文章
user    1──N comments        一个用户 ← 多条批注
article 1──1 savings         一篇存档 ← 一份存储快照
savings 1──N saving_items    一份快照 ← N 个文件+目录条目
article 1──N comments        一篇存档 ← 多条批注
article 1──N download_logs   一篇存档 ← 多条下载记录
```

### 表结构概要

| 表 | 关键字段 | 说明 |
|----|----------|------|
| `users` | id, username(UK), password(BCrypt), role | 管理员/用户角色分离 |
| `games` | id, name(UK) | 游戏主数据 |
| `article` | id, title, version, readme_raw/readme_content(LONGTEXT), status, storage_root | 核心业务实体 |
| `savings` | id, article_id(FK), zip_path, extract_root, zip_hash(SHA-256) | 存储快照 |
| `saving_items` | snapshot_id(FK), virtual_path, physical_key({md5}.{ext}), parent_path, is_directory | 文件+目录统一存储 |
| `comments` | article_id, user_id, content, anchor(JSON) | 批注 |
| `download_logs` | article_id, ip_address, downloaded_at | 下载审计 |

完整建库脚本见 `gamesaving_database.sql`（含注释 + 测试数据）。

---

## 四、混合存储物理结构

```
Database/{user_id}/{game_id}/{article_id}/
  ├── archive.zip          ← 原始上传压缩包
  └── extracted/           ← 解压根目录（内容寻址）
        ├── {md5}.dat
        ├── {md5}.mca
        └── ...
```

文件去重：相同 MD5 → 相同 physical_key → 只存一份。

---

## 五、核心查询模式

```sql
-- GitHub 风格：根目录文件列表
SELECT * FROM saving_items
WHERE snapshot_id=? AND parent_path='' AND is_directory=0;

-- GitHub 风格：进入子目录
SELECT * FROM saving_items
WHERE snapshot_id=? AND parent_path='region/' AND is_directory=0;

-- 浏览列表：某游戏的 READY 存档
SELECT a.*, u.nickname FROM article a
JOIN users u ON a.user_id=u.id
WHERE a.game_id=? AND a.status='READY'
ORDER BY a.created_at DESC LIMIT 20;

-- Word 风格：加载批注
SELECT c.*, u.nickname FROM comments c
JOIN users u ON c.user_id=u.id
WHERE c.article_id=? ORDER BY c.created_at;

-- 文件预览：virtual_path → physical_key
SELECT physical_key, file_type, is_text
FROM saving_items WHERE snapshot_id=? AND virtual_path=?;
```

---

## 六、安全要求

- **文件上传**：前后端双重限制 200MB、ZIP 魔数校验、路径穿越防御
- **解压安全**：异步任务超时处理、失败标记 FAILED、路径穿越检查
- **XSS 防御**：批注内容 + README 均需清洗
- **下载限流**：ConcurrentHashMap（内存）+ download_logs（数据库）双层

---

## 七、已完成功能清单 (v1.0)

### 用户系统
- [x] 注册 / 登录（用户名或手机号 + BCrypt 密码）
- [x] 用户角色分离（admin / user）
- [x] 用户信息查看 / 编辑
- [x] 导航守卫（未登录重定向）

### 游戏管理
- [x] 游戏列表（首页）
- [x] 游戏 CRUD
- [x] 按游戏浏览存档

### 存档上传与处理
- [x] ZIP 文件上传（multipart，最大 200MB）
- [x] README 双模式：手写 Markdown + 上传 .md 文件
- [x] 异步 ZIP 解压（内容寻址存储、自动目录节点）
- [x] UTF-8 / GBK 文件名编码兼容
- [x] 处理状态轮询（UPLOADING → EXTRACTING → READY / FAILED）
- [x] 解压超时与失败处理

### 文件浏览与下载
- [x] GitHub 风格文件目录浏览（面包屑导航）
- [x] 文件元数据查看
- [x] 文本文件在线预览（highlight.js 代码高亮）
- [x] ZIP 原始文件下载
- [x] 双层 IP 下载限流（内存 + 数据库）

### 批注系统
- [x] 批注 CRUD
- [x] 批注按文章查询（带用户昵称 join）
- [x] 文字颜色高亮（CSS Custom Highlight API + 极简 DOM 边框层）
- [x] 角色颜色区分（管理员红 / 上传者蓝 / 普通用户绿）
- [x] 批注卡片与被批注文字同一 Y 轴定位
- [x] 卡片折叠/展开（原文引用 + 内容预览 + 角色标签 + 删除）
- [x] 提交/删除后页面自动刷新 + sessionStorage 保持滚动位置

### Markdown 渲染
- [x] README.md 渲染为 HTML
- [x] readme_raw（源）+ readme_content（渲染缓存）双字段

---

## 八、未来需要实现的功能

### 高优先级（毕业设计答辩必需）

#### 1. 用户认证升级
- [ ] **JWT Token 认证**：替换当前 localStorage 存用户对象的方案
- [ ] **Token 刷新机制**：access_token (15min) + refresh_token (7d)
- [ ] **Spring Security 集成**：`SecurityFilterChain` + `OncePerRequestFilter`
- [ ] **权限控制**：`@PreAuthorize` 注解，管理员可删任意内容

#### 2. Markdown 渲染升级
- [ ] **替换为正版 Markdown 解析器**：引入 `commonmark` 或 `flexmark` 替代当前简单正则 `MarkdownRenderer`
- [ ] **GFM 支持**：表格、任务列表、删除线
- [ ] **图片懒加载**：README 中图片的 CDN + loading="lazy"

#### 3. 批注系统完善
- [x] **前端批注 UI**：`@recogito/text-annotator` 集成到 `ReadmeRenderer`，实现选中文本弹出批注框
- [x] **批注锚定持久化**：W3C TextQuoteSelector + TextPositionSelector，页面刷新后精确还原高亮位置
- [x] **文字角色颜色高亮**：CSS Custom Highlight API（背景+虚线下划线）+ 极简 DOM 边框层（左边色条）
- [ ] **批注回复**：支持评论嵌套（一级回复）
- [ ] **批注通知**：被回复用户收到通知

#### 4. 文件系统增强
- [ ] **文件下载改为 COS 预签名 URL**：下载不经过 ECS，直连对象存储
- [ ] **图片/二进制预览**：图片文件在线预览（非文本文件也能看缩略图/hex dump）
- [ ] **目录树侧边栏**：类似 VS Code 的文件树，替代当前面包屑式导航
- [ ] **多文件上传**：支持非 ZIP 格式（.rar, .7z, .tar.gz）

#### 5. 搜索与发现
- [ ] **全文搜索**：Elasticsearch / MySQL FULLTEXT 索引，搜索存档标题+描述+README
- [ ] **标签系统**：article_tags 表，按玩法/模组/版本分类
- [ ] **热门存档**：首页展示下载量 Top 10

#### 6. 批注系统下一阶段（2026-06-21 规划）

##### 6.1 每日批注限制与修改次数
- [ ] **每日 1 次提交限制**：同一用户同一文章每天只能创建 1 条批注，超限返回 429
- [ ] **2 次修改机会**：comment 表新增 `edit_count` 字段，`PUT /api/comments/{id}` 编辑接口
- [ ] **前后端校验**：后端查 COUNT + edit_count，前端显示剩余次数

##### 6.2 显示/隐藏批注开关
- [ ] **全局批注开关**：按钮切换显示/隐藏所有批注（纯前端，不涉及后端）
- [ ] **隐藏时文章居中**：侧边栏收起 → `.article-main` 居中，`<Transition>` 平滑动画
- [ ] **高亮同步隐藏**：HighlightManager + BorderLayer + AnnotationPanel 同步切换

##### 6.3 回到顶部按钮
- [ ] **全局浮动按钮**：`position: fixed` 右下角，scrollY > 500 时淡入
- [ ] **平滑滚动**：`window.scrollTo({ top: 0, behavior: 'smooth' })`

##### 6.4 用户体验优化
- [ ] **卡片与文字联动高亮**：hover 批注卡片 → 对应文字高亮加深
- [ ] **键盘快捷键**：`Ctrl+B` 切换批注显隐（已有 Esc 关闭弹窗、Ctrl+Enter 提交）
- [ ] **空状态优化**：无批注时显示 `<el-empty>` 引导插图
- [ ] **批注编辑**：卡片新增"编辑"按钮，复用 AnnotationPopup 输入框

### 中优先级（体验优化）

#### 6. 用户系统完善
- [ ] **头像上传**：集成 COS 存储用户头像
- [ ] **邮箱验证**：注册时发送验证码
- [ ] **密码找回**：邮箱重置密码
- [ ] **用户关注**：follow 表，关注后可收到新存档通知

#### 7. 协作功能
- [ ] **存档协作**：允许多人共同维护一个存档条目（类似 GitHub Collaborator）
- [ ] **版本历史**：支持同一存档的多个版本上传，版本时间线查看
- [ ] **Diff 对比**：两个版本间文件变更对比

#### 8. 国际化
- [ ] **i18n 支持**：中/英文切换（vue-i18n）
- [ ] **多语言存档**：存档描述支持多语言版本

#### 9. 性能优化
- [ ] **分页优化**：大存档列表游标分页替代 offset 分页
- [ ] **CDN 加速**：静态资源（Vue 打包产物、图片）通过 CDN 分发
- [ ] **缓存策略**：Redis 缓存热门游戏列表、存档详情

### 低优先级（锦上添花）

#### 10. 社交功能
- [ ] **点赞/收藏**：article_likes 表
- [ ] **分享**：生成分享卡片（Open Graph meta tags）
- [ ] **评论**：文章级别评论（区别于 README 批注）

#### 11. 管理后台
- [ ] **独立管理面板**：`/admin` 路由，仅 admin 可见
- [ ] **用户管理**：封禁/解封用户
- [ ] **存档审核**：违规内容举报 + 审核

#### 12. DevOps
- [ ] **Docker 化**：Dockerfile + docker-compose（Spring Boot + MySQL + Nginx）
- [ ] **CI/CD**：GitHub Actions 自动化测试 + 部署
- [ ] **监控**：Spring Boot Actuator + Prometheus + Grafana
- [ ] **日志收集**：ELK Stack

---

## 九、数据库连接信息

```
Host: localhost:3306
Database: gamesaving
User: root
Password: 3256
Charset: utf8mb4
JPA ddl-auto: validate（表由 SQL 脚本手动创建）
```

## 十、项目文件索引

| 文件 | 说明 |
|------|------|
| `gamesaving_database.sql` | 完整建库脚本（7 表 + 测试数据） |
| `test_data_insert.sql` | Minecraft 存档测试数据 |
| `CLAUDE.md` | AI 辅助开发指南 |
| `Backend/GameSaves/BE_README.md` | 后端架构文档 |
| `Frontend/GameSaves_Fronted/FE_README.md` | 前端架构文档 |
| `docs/ecs-cos-split-storage.md` | ECS+COS 分拆存储架构构想 |
