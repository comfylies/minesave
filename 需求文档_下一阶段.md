# 游戏存档分享平台 — 下一阶段需求文档

> 整理日期: 2026-06-23
> 来源: 现有功能审查 + 行业方案调研

---

## 一、搜索系统升级

### 1.1 现状

主页搜索为纯前端 `name.includes(kw)` + `description.includes(kw)` 遍历，游戏全量加载无分页。

### 1.2 问题

- 不支持模糊匹配、拼音、错别字容忍
- 无全文索引，数据量大时性能下降
- 游戏内无存档搜索功能

### 1.3 方案

| 搜索场景 | 方案 | 优先级 |
|----------|------|--------|
| 主页游戏搜索 | 后端 MySQL FULLTEXT INDEX 或 Elasticsearch，API 分页搜索 | P2 |
| 游戏内存档搜索 | 对 article 的 `title` + `description` 建联合索引，后端模糊搜索 API | P1 |
| 存档内容识别 | 上传时引导填写描述 + README + Tag（见下文），不依赖全文检索 README | P0 |

---

## 二、存档辨识度提升

### 2.1 问题

存档名如"我的世界存档 v1"信息量极低。存档多了仅看标题无法区分内容。

### 2.2 方案：三层信息结构

| 层 | 内容 | 说明 |
|----|------|------|
| **封面图** | 上传时指定或从解压文件中提取 | 视觉辨识，一图胜千言 |
| **描述** | 上传时必填（提供模板引导） | 如"1.20.1 生存模式，已击败末影龙" |
| **Tag 标签** | 预设 + 自定义（见下文） | 结构化分类，精准搜索 |

### 2.3 画廊视图

在列表视图外新增**画廊（卡片）视图**：

```
┌──────────────┐
│   封面图片    │
├──────────────┤
│ 存档标题      │
│ 🏷️ 生存 🏷️ 1.20 │
│ 2024-06-23   │
└──────────────┘
```

- 游戏页顶部加 `[ 📋 列表 ] [ 🖼️ 画廊 ]` 切换按钮
- `article` 表新增 `cover_image VARCHAR(500)` 字段
- 上传时支持单独上传封面图，存储到 `Database/{userId}/{gameId}/{articleId}/cover.png`
- 通过现有 `/storage/**` 映射访问

---

## 三、Tag 标签系统

### 3.1 数据库

```sql
CREATE TABLE tags (
    id   BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) UNIQUE NOT NULL
);

CREATE TABLE article_tags (
    article_id BIGINT NOT NULL,
    tag_id     BIGINT NOT NULL,
    PRIMARY KEY (article_id, tag_id),
    FOREIGN KEY (article_id) REFERENCES article(id),
    FOREIGN KEY (tag_id) REFERENCES tags(id)
);
```

### 3.2 标签来源

| 类型 | 维护者 | 示例 |
|------|--------|------|
| **预设标签** | 管理员 | `生存模式` `创造模式` `红石机器` `建筑` |
| **用户标签** | 上传者自定义 | `村民交易大厅` `猪灵交易` `全自动农场` |

### 3.3 搜索联动

前端搜索时 Tag + 标题 + 描述联合查询。Tag 提供结构化高辨识度信息。

---

## 四、安全方案（防病毒）

### 4.1 设计原则

- 服务器**只解压不执行**，这是根本安全前提
- 不过度拦截——Minecraft 服务端自带 `.sh` 启动脚本，禁用会破坏正常用途
- 核心目标：防止伪装文件诱导用户下载执行

### 4.2 第一层：前端文件颜色标记（FileBrowser 组件）

| 颜色 | 文件类型 | 含义 |
|------|----------|------|
| 🔴 红色 | `.exe` `.bat` `.cmd` `.vbs` `.ps1` `.scr` `.msi` | 高度可疑，双击即执行 |
| 🟡 黄色 | `.sh` `.dll` `.so` `.jar` `.py` `.rb` | 可执行/动态库，提醒注意 |
| ⚪ 默认 | 其余后缀 | 存档数据文件 |

- 纯前端改动，在 `FileBrowser.vue` / `FileIcon.vue` 中按扩展名加 CSS class
- 服务器不做任何拦截，仅告知用户"此文件可能可执行"

### 4.3 第二层：后端魔数校验（ZipExtractor 嵌入）

**规则：扩展名声称的类型 与 文件头魔数 不一致 → 拒绝上传，标记 FAILED**

| 魔数 | 真实类型 | 常见伪装 |
|------|----------|----------|
| `MZ` (0x4D 0x5A) | Windows PE | 伪装成 `.txt` `.jpg` |
| `7F ELF` (0x7F 0x45 0x4C 0x46) | Linux ELF | 伪装成 `.cfg` `.dat` |
| `#!` (shebang) | 文本脚本 | `.sh` 本身即文本，不以此拦截 |

**实现要点：**
- 在 `ZipExtractor` 中，`FileHasher` 计算 MD5 之后、写入磁盘之前插入校验
- 读取每个文件头 4 字节，与扩展名期望类型对比
- 不匹配则拒绝整个存档（或拒绝该文件 + 记录日志）
- 纯 JDK 实现，零外部依赖

---

## 五、游戏数量扩展

### 5.1 问题

世界上有上万个游戏，管理员不可能穷举。但允许用户完全自由创建会导致 `Minecraft` / `我的世界` / `MC` 重复条目泛滥。

### 5.2 方案：两级兼容 + 别名合并

| 层级 | 来源 | 说明 |
|------|------|------|
| **热门游戏** | 管理员维护 | 有封面、描述、官方名称 |
| **IGDB/RAWG API 辅助** | 第三方 API（P2） | 输入时实时搜索补全 |
| **用户自定义** | 上传者填写 | API 中找不到的冷门游戏，标记 `unverified` |

### 5.3 数据库改动

```sql
ALTER TABLE games ADD COLUMN canonical_id BIGINT NULL
    COMMENT '指向标准名称的 game id，非空时为别名';
ALTER TABLE games ADD COLUMN source VARCHAR(20) DEFAULT 'user'
    COMMENT 'admin | igdb | user';
```

### 5.4 别名处理逻辑

```
Minecraft（admin 创建，canonical_id=NULL, source='admin'）
  ← 我的世界（用户创建，canonical_id=指向Minecraft, source='user'）
  ← MC（用户创建，canonical_id=指向Minecraft, source='user'）

搜索时：canonical_id 不为空的游戏自动指向标准游戏的文章列表
展示时：显示 "我的世界"（别名 → Minecraft）
```

### 5.5 前端输入框

类似 GitHub label 选择器——输入文字 → 实时搜索本地数据库 → 下拉列表：
- 匹配到已有游戏：选中关联
- 未匹配到：显示"创建新游戏：xxx"
- 管理员定期合并 `unverified` 的重复游戏

---

## 六、优先级汇总

| 优先级 | 功能 | 说明 | 预估改动 |
|--------|------|------|----------|
| **P0** | Tag 标签系统 | 极大提升辨识度和搜索体验 | 2 表 + 后端 CRUD + 前端 |
| **P0** | 封面图 + 画廊视图 | 视觉辨识，不依赖 README | article +1 字段 + 前端 |
| **P0** | 魔数校验 | 安全底线，拦截伪装文件 | ZipExtractor ≈50 行 |
| **P1** | 文件颜色标记 | 用户知情权，纯前端 | FileBrowser ≈30 行 |
| **P1** | 游戏内存档搜索 | 后端搜索 API | 后端索引 + API |
| **P1** | 用户自定义游戏名 + 别名 | 解决游戏数量瓶颈 | games +2 字段 + 前端 |
| **P2** | IGDB API 集成 | 减少重复，提高数据质量 | API 对接 + 前端搜索框 |
| **P2** | 后端全文搜索 | MySQL FULLTEXT 或 ES | 索引 + 搜索 API |

---

## 七、不改动清单

以下功能经评估**不实施**：

| 功能 | 原因 |
|------|------|
| ClamAV 杀毒 | 服务器不执行文件，魔数校验已足够，ClamAV 引入重量级依赖 |
| 禁用 `.sh`/可执行文件上传 | Minecraft 服务端自带启动脚本，禁用对小白用户不友好 |
| 全文检索 README 内容 | 封面 + Tag + 描述已提供充分信息，检索 README 耗费资源 |
| Docker 化 / CI/CD | 毕业设计阶段不需要 |
