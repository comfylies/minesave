# 游戏存档分享平台 — 下一阶段需求文档

> 整理日期: 2026-06-23
> 来源: 现有功能审查 + 行业方案调研

---

## 一、搜索系统升级 ✅ 已完成 (2026-06-23)

### 1.1 实现方案：Meilisearch 统一搜索

采用 **Meilisearch** 搜索引擎（Rust 编写，轻量二进制 ~30MB），零 Java 依赖之外的中间件。

### 1.2 搜索能力

| 能力 | 实现方式 |
|------|----------|
| 统一搜索 | 游戏 + 存档在同一索引 `saves`，单次查询返回混合结果 |
| 错别字容忍 | Meilisearch `typoTolerance` 内置，中英文均支持 |
| 中文搜索 | CJK tokenizer 原生支持，比 MySQL ngram 更精准 |
| 相关性排序 | 下载量高 > 发布时间新 > 匹配度 |
| 即输即搜 | <50ms 响应，导航栏搜索框 → `/search?q=` |
| 高亮 | 匹配词以 `<em>` 标签高亮返回 |

### 1.3 已实现内容

| 层 | 内容 |
|----|------|
| Infra | Meilisearch 进程 localhost:7700，索引 `saves` |
| Backend | `SearchService`（查询）+ `SearchSyncService`（数据同步）+ `SearchController`（`GET /api/search`） |
| 同步 | Article/Game CRUD 时自动同步索引，启动时全量重建，Admin 面板可手动触发 reindex |
| Frontend | 导航栏统一搜索框 + `SearchPage.vue` 搜索结果页（按类型分组、高亮、分页） |
| HomePage | 删除原 `includes()` 遍历搜索，搜索统一走导航栏 |

### 1.4 为什么没选 MySQL FULLTEXT / ES

| 方案 | 不选原因 |
|------|----------|
| MySQL FULLTEXT | 无错别字容忍、无跨表统一搜索、ngram 二分词精度不够 |
| Elasticsearch | 重量级（JVM 堆 1GB+），毕业设计过度设计 |
| Meilisearch | 30MB 二进制、REST API、<50ms、CJK + typo 开箱即用 ✅ |

---

## 二、存档辨识度提升 ✅ 已完成 (2026-06-24)

### 2.1 问题

存档名如"我的世界存档 v1"信息量极低。存档多了仅看标题无法区分内容。

### 2.2 方案：三层信息结构

| 层 | 内容 | 说明 |
|----|------|------|
| **封面图** | 上传时指定或从解压文件中提取 | 视觉辨识，一图胜千言 |
| **描述** | 上传时必填（提供模板引导） | 如"1.20.1 生存模式，已击败末影龙" |
| **Tag 标签** | 预设 + 自定义（见下文） | 结构化分类，精准搜索 |

### 2.3 画廊视图

在列表视图外新增**画廊（卡片）视图**：

```
┌──────────────┐
│   封面图片    │
├──────────────┤
│ 存档标题      │
│ 🏷️ 生存 🏷️ 1.20 │
│ 2024-06-23   │
└──────────────┘
```

- 游戏页顶部加 `[ 📋 列表 ] [ 🖼️ 画廊 ]` 切换按钮
- `article` 表新增 `cover_image VARCHAR(500)` 字段
- 上传时支持单独上传封面图，存储到 `Database/{userId}/{gameId}/{articleId}/cover.png`
- 通过现有 `/storage/**` 映射访问

---

## 三、Tag 标签系统 ✅ 已完成 (2026-06-23)

### 3.1 数据库

```sql
CREATE TABLE tags (
    id         BIGINT AUTO_INCREMENT PRIMARY KEY,
    name       VARCHAR(50) UNIQUE NOT NULL,
    source     VARCHAR(20) NOT NULL DEFAULT 'user' COMMENT 'admin=预设标签, user=用户自定义',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

CREATE TABLE article_tags (
    article_id BIGINT NOT NULL,
    tag_id     BIGINT NOT NULL,
    PRIMARY KEY (article_id, tag_id),
    FOREIGN KEY (article_id) REFERENCES article(id) ON DELETE CASCADE,
    FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE
);
```

### 3.2 已实现内容

| 层 | 内容 |
|----|------|
| 后端 | Tag 实体 + Repository + Service + Controller（`/api/tags` 公开端点）、Admin 管理端点（需 `tag:manage` 权限）、Article 多对多关联、上传/编辑时支持 tagIds |
| 前端 | TagSelector（多选 + 分组 + 即时创建）、TagDisplay（最多显示 2 个标签，超出 +N）、UploadPage / GamePage / ArticleMeta 集成 |
| 测试 | TagServiceTest 13 个用例全部通过 |

### 3.3 标签来源

| 类型 | 维护者 | 示例 |
|------|--------|------|
| **预设标签** | 管理员 | `生存模式` `创造模式` `红石机器` `建筑` |
| **用户标签** | 上传者自定义 | `村民交易大厅` `猪灵交易` `全自动农场` |

### 3.4 搜索联动

前端搜索时 Tag + 标题 + 描述联合查询。Tag 提供结构化高辨识度信息。

---

## 四、安全方案（防病毒）

### 4.1 设计原则

- 服务器**只解压不执行**，这是根本安全前提
- 不过度拦截——Minecraft 服务端自带 `.sh` 启动脚本，禁用会破坏正常用途
- 核心目标：防止伪装文件诱导用户下载执行

### 4.2 第一层：前端文件颜色标记（FileBrowser 组件）✅ 已实现 (2026-06-24)

**实现方案：标准结构白名单 + 扩展名分级**

管理员上传游戏标准文件夹结构 ZIP → 提取路径存入 `safe_paths` 表作为白名单基线。
浏览存档时，后端交叉比对文件路径与白名单，返回 `securityLevel`（safe/warning/danger）。

| 等级 | 视觉 | 触发条件 |
|------|------|----------|
| 🔴 danger | 红色左边框 + 淡红背景 + "⚠ 可疑" 徽章 | `.exe/.bat/.cmd/.vbs/.ps1/.scr/.msi` 永远红色；`.sh/.py/.rb` 不在标准结构中 |
| 🟡 warning | 黄色左边框 + 淡黄背景 + "注意" 徽章 | `.sh/.py/.rb` 在标准结构中（正常脚本）；`.dll/.so/.jar` 不在标准结构中（可疑位置） |
| ⚪ safe | 默认样式 | 其余所有文件 |

**决策矩阵：**

| 扩展名 | 在标准结构中 | 不在标准结构中 |
|--------|-------------|---------------|
| `.exe .bat .cmd .vbs .ps1 .scr .msi` | 🔴 danger | 🔴 danger |
| `.sh .py .rb` | 🟡 warning | 🔴 danger |
| `.dll .so .jar` | ⚪ safe | 🟡 warning |
| 其余 | ⚪ safe | ⚪ safe |

**已实现内容：**

| 层 | 内容 |
|----|------|
| DB | 新表 `safe_paths(game_id, path, is_directory)` + `uk_game_path` 唯一索引 |
| 后端 | `SafePath` 实体 + `SafePathRepository` + `SafePathService`（上传解析 ZIP、查询白名单 Set） |
| 后端 | `FileEntryResponse.securityLevel` 字段 + `FileExplorerServiceImpl.computeSecurityLevel()` 决策逻辑 |
| 后端 | `POST /api/admin/games/{gameId}/safe-structure`（上传标准结构 ZIP）+ `GET` 查询端点 |
| 前端 | `FileBrowser.vue` 行级安全样式（左色条 + 背景 + 徽章）+ hover tooltip + 底部图例 |

**部署步骤：**
1. 执行 `gamesaving_database.sql` 中新增的 `safe_paths` 建表语句
2. 管理员在后台为游戏上传标准结构 ZIP

### 4.3 第二层：后端魔数校验（ZipExtractor 嵌入）✅ 已完成 (2026-06-24)

**规则：扩展名声称的类型 与 文件头魔数 不一致 → 拒绝上传，标记 FAILED**

| 魔数 | 真实类型 | 常见伪装 |
|------|----------|----------|
| `MZ` (0x4D 0x5A) | Windows PE | 伪装成 `.txt` `.jpg` |
| `7F ELF` (0x7F 0x45 0x4C 0x46) | Linux ELF | 伪装成 `.cfg` `.dat` |
| `#!` (shebang) | 文本脚本 | `.sh` 本身即文本，不以此拦截 |

**实现要点：**
- 在 `ZipExtractor` 中，`FileHasher` 计算 MD5 之后、写入磁盘之前插入校验
- 读取每个文件头 4 字节，与扩展名期望类型对比
- 不匹配则拒绝整个存档（或拒绝该文件 + 记录日志）
- 纯 JDK 实现，零外部依赖

---

## 五、失败存档清理机制 ✅ 已完成 (2026-06-24)

### 5.1 问题

上传失败的存档（ZIP 损坏、解压超时、魔数校验拦截）会遗留物理文件和数据库记录，当前无自动清理机制。磁盘空间被无效文件持续占用。

### 5.2 清理目标

| 状态 | 条件 | 清理时机 |
|------|------|----------|
| `FAILED` | `updated_at < NOW() - 3h` | 定时扫描 |
| `UPLOADING` | `updated_at < NOW() - 3h`（上传中断未恢复） | 定时扫描 |

### 5.3 触发方式

| 方式 | 说明 |
|------|------|
| **定时触发** | `@Scheduled` cron 表达式，默认每 3 小时一次 |
| **启动触发** | `@PostConstruct` 服务启动时立即执行一次 |
| **手动触发** | `POST /api/admin/cleanup/trigger?mode=all|failed-only` |

### 5.4 模式

| 模式 | 行为 |
|------|------|
| `all`（默认） | 清理 FAILED + 过期 UPLOADING |
| `failed-only` | 仅清理 FAILED，保留 UPLOADING 记录 |

### 5.5 恶意攻击防护：分批清理

**问题：** 如果有人在 3 小时内恶意上传 5000 个失败 ZIP，定时扫描一次查出 5000 条——单事务处理会：
- 一次加载 5000 条记录 → 内存压力
- 逐个 `Files.deleteRecursively()` → I/O 阻塞数分钟
- 单事务内全量操作 → 长时间锁表

**方案：内存分批 + 小事务，不需要消息队列**

```
每批 50 条 → 独立事务 → sleep 2s → 下一批 → 循环直到清完
```

```
┌──────────────────────────────────────────┐
│  CleanupScheduler                         │
│                                           │
│  totalDeleted = 0                         │
│  while (true) {                           │
│    page = SELECT ... LIMIT 50             │
│    if (page.isEmpty) break                │
│    for each article in page {             │
│      deletePhysicalFiles(article)  // I/O  │
│      articleRepo.delete(article)   // DB   │
│    }                                      │
│    totalDeleted += page.size              │
│    sleep(2s)  ← 给其他请求让出 I/O        │
│  }                                        │
│  log.info("清理完成: {} 条", totalDeleted) │
│                                           │
│  → 每批独立 @Transactional，事务短         │
│  → sleep 释放 I/O 和 DB 连接              │
│  → 5000 条也分批消化，不会卡死             │
└──────────────────────────────────────────┘
```

### 5.6 进度查询

```
GET /api/admin/cleanup/status
→ { "running": true, "batchesCompleted": 12, "totalDeleted": 600, "estimatedRemaining": 200 }
```

### 5.7 配置项

```yaml
app:
  cleanup:
    cron: "0 0 */3 * * *"          # 默认 3 小时，运维可通过环境变量覆盖
    batch-size: 50                 # 每批清理数量
    batch-delay-ms: 2000           # 批次间休眠时间
    retention-hours: 3             # FAILED / 过期 UPLOADING 保留时长
    delete-physical-files: true    # 是否删除磁盘文件（false 则仅清 DB 记录）
```

### 5.8 安全边界

| 措施 | 说明 |
|------|------|
| 状态白名单 | 仅清理 `FAILED` 和 `UPLOADING`，绝不触碰 `READY` 和 `EXTRACTING` |
| 时间阈值 | 3h 内即使 FAILED 也不删——保留足够现场用于排查 |
| 手动触发限流 | `POST /api/admin/cleanup/trigger` 60s 内仅允许一次，防止连续点击 |
| 操作日志 | 每次清理记录日志：清理数量、耗时、具体 article ID 列表 |

### 5.9 预估改动

| 层 | 改动 |
|----|------|
| 后端 | 1 个新类 `CleanupScheduler`（~80 行）、AdminController 新增 2 个端点、`application.yaml` 新增配置块 |
| 前端 | Admin 面板新增清理页面（状态卡片 + 触发按钮） |

---

## 六、游戏数量扩展

### 6.1 问题

世界上有上万个游戏，管理员不可能穷举。但允许用户完全自由创建会导致 `Minecraft` / `我的世界` / `MC` 重复条目泛滥。

### 6.2 方案：两级兼容 + 别名合并

| 层级 | 来源 | 说明 |
|------|------|------|
| **热门游戏** | 管理员维护 | 有封面、描述、官方名称 |
| **IGDB/RAWG API 辅助** | 第三方 API（P2） | 输入时实时搜索补全 |
| **用户自定义** | 上传者填写 | API 中找不到的冷门游戏，标记 `unverified` |

### 6.3 数据库改动

```sql
ALTER TABLE games ADD COLUMN canonical_id BIGINT NULL
    COMMENT '指向标准名称的 game id，非空时为别名';
ALTER TABLE games ADD COLUMN source VARCHAR(20) DEFAULT 'user'
    COMMENT 'admin | igdb | user';
```

### 6.4 别名处理逻辑

```
Minecraft（admin 创建，canonical_id=NULL, source='admin'）
  ← 我的世界（用户创建，canonical_id=指向Minecraft, source='user'）
  ← MC（用户创建，canonical_id=指向Minecraft, source='user'）

搜索时：canonical_id 不为空的游戏自动指向标准游戏的文章列表
展示时：显示 "我的世界"（别名 → Minecraft）
```

### 6.5 前端输入框

类似 GitHub label 选择器——输入文字 → 实时搜索本地数据库 → 下拉列表：
- 匹配到已有游戏：选中关联
- 未匹配到：显示"创建新游戏：xxx"
- 管理员定期合并 `unverified` 的重复游戏

---

## 七、优先级汇总

| 优先级 | 功能 | 说明 | 预估改动 | 状态 |
|--------|------|------|----------|------|
| ~~P0~~ | Tag 标签系统 | 极大提升辨识度和搜索体验 | 2 表 + 后端 CRUD + 前端 | ✅ 已完成 |
| ~~P0~~ | 封面图 + 画廊视图 | 视觉辨识，不依赖 README | article +1 字段 + 前端 | ✅ 已完成 |
| ~~P0~~ | 魔数校验 | 安全底线，拦截伪装文件 | ZipExtractor + 配置 | ✅ 已完成 (2026-06-24) |
| ~~P1~~ | 失败存档清理 | 自动清理 FAILED/过期 UPLOADING，防磁盘占满 | 1 类 + Admin 端点 + 前端页面 | ✅ 已完成 (2026-06-24) |
| ~~P1~~ | 文件颜色标记 | 用户知情权，标准结构白名单 + 扩展名分级 | 1 表 + 后端服务 + 前端 | ✅ 已完成 |
| ~~P1~~ | 游戏内存档搜索 | Meilisearch 统一搜索，错别字容忍，游戏+存档合并结果 | Meilisearch + 后端 API + 前端搜索页 | ✅ 已完成 |
| **P1** | 用户自定义游戏名 + 别名 | 解决游戏数量瓶颈 | games +2 字段 + 前端 | |
| **P2** | IGDB API 集成 | 减少重复，提高数据质量 | API 对接 + 前端搜索框 | |
| **P2** | 后端全文搜索 | MySQL FULLTEXT 或 ES | 索引 + 搜索 API | |

---

## 八、不改动清单

以下功能经评估**不实施**：

| 功能 | 原因 |
|------|------|
| ClamAV 杀毒 | 服务器不执行文件，魔数校验已足够，ClamAV 引入重量级依赖 |
| 禁用 `.sh`/可执行文件上传 | Minecraft 服务端自带启动脚本，禁用对小白用户不友好 |
| 全文检索 README 内容 | 封面 + Tag + 描述已提供充分信息，检索 README 耗费资源 |
| Docker 化 / CI/CD | 毕业设计阶段不需要 |
