# GameSaving 测试文档

> 最后更新：2026-06-28 | 测试数：129 | 前端工具函数已去重（`utils/format.js`）

---

## 一、当前状态

```
BUILD SUCCESS — Tests: 129, Failures: 0, Errors: 0
```

## 二、测试分类

### 2.1 后端单元测试（纯逻辑，不需要 Spring）

| 测试类 | 数量 | 被测对象 | 覆盖要点 |
|--------|------|----------|----------|
| `PathTraversalValidatorTest` | 25 | `PathTraversalValidator` | 正常路径、空值、`../`、绝对路径(`/etc/passwd`)、Windows 盘符(`C:\`)、`./` 穿越、`computeParentPath` |
| `XssFilterTest` | 12 | `XssFilter` | `<script>`、`onerror`、`javascript:`、HTML 实体 |
| `MagicNumberValidatorTest` | 25 | `MagicNumberValidator` | MZ/ELF/shebang 检测、扩展名白名单、enabled/disabled |
| `ZipExtractorTest` | 11 | `ZipExtractor` | 正常解压、空ZIP、目录条目、路径穿越拒绝、ZIP炸弹、流式SHA-256、大文件(>10MB)临时文件路径 |

### 2.2 后端集成测试（需要 MySQL + Spring 上下文）

| 测试类 | 数量 | 被测对象 | 覆盖要点 |
|--------|------|----------|----------|
| `AuthServiceTest` | 9 | `AuthService` | **H-1**: 错误密码/不存在用户统一返回 "Invalid username or password"；5次失败锁定(AccountLockedException)；**M-1**: 验证码强制(null key/code拒)；**H-3**: 邮箱码发送需验证码；注册重名统一消息；禁用账号拒登 |
| `CommentServiceTest` | 6 | `CommentServiceImpl` | **H-5**: 创建批注使用currentUserId(非request参数)；删除自己的批注成功；非作者删除被拒("Only the comment author or an admin")；admin可删他人；批注列表查询 |
| `FileExplorerServiceTest` | 11 | `FileExplorerServiceImpl` | **H-2**: browse/path 路径穿越拒绝(getFileDetail/getFileContent)；**2.4**: preview白名单(.json/.conf通过，.mca/.png拒绝)；正常浏览根目录/子目录 |
| `LocalStorageServiceImplTest` | 10 | `LocalStorageServiceImpl` | 存储/读取/删除/存在检查/目录删除/路径穿越 |
| `CleanupSchedulerTest` | 6 | Cleanup 调度 | 清理定时任务逻辑 |
| `TagServiceTest` | 13 | TagService | 标签 CRUD |
| `GameSavesApplicationTests` | 1 | Spring 上下文 | 冒烟测试 |

### 2.3 前端测试

**当前 0 测试。** 日后补：

| 层级 | 推荐工具 | 测什么 |
|------|----------|--------|
| Store 逻辑 | Vitest + pinia testing | `authStore.logout()` 调用其他 store reset；登录流程 |
| 组件 | Vitest + @vue/test-utils | LoginForm 验证码字段、FileBrowser 面包屑渲染 |
| E2E | Playwright | 注册→上传→预览→批注→下载 全流程 |

---

## 三、已知缺口

### 3.1 后端

| 缺口 | 原因 | 待办 |
|------|------|------|
| AuthService 登录成功/注册成功 | `StpUtil.login()` 需要 Sa-Token Web 上下文，纯 Service 层无法初始化 | 用 MockMvc 写 API 层集成测试 |
| 邮箱码登录完整流程 | `EmailCodeService` 依赖 QQ SMTP，无法在测试中真实发邮件 | Mock `EmailCodeService` 或使用 GreenMail |
| FileExplorerService `getFileContent` 大小超限 | 测试数据中没有超 5MB 的文本文件 | 插入大测试文件或 Mock StorageService |

### 3.2 前端

| 缺口 | 待办 |
|------|------|
| 完全无测试 | `npm install -D vitest @vue/test-utils jsdom` → 先补 Store 测试 |

---

## 四、如何运行

```bash
# 前提：MySQL 8.0 运行中，数据库 gamesaving 已初始化
# 全部测试
cd Backend/GameSaves && ./mvnw test

# 单个测试类
./mvnw test -Dtest=AuthServiceTest
./mvnw test -Dtest=PathTraversalValidatorTest

# 前端（待实现）
cd Frontend/GameSaves_Fronted && npx vitest run
```

## 五、数据库测试数据

测试依赖 `gamesaving_database.sql` + `test_data_insert.sql` 中的预置数据：

| 用户 | 密码 | 角色 |
|------|------|------|
| admin (id=1) | password123 | admin |
| player_one (id=2) | password123 | user |
| speedrunner (id=3) | password123 | user |
| minecraft_user (id=4) | password123 | user |

测试存档：article_id=1（READY，savings_id=1，50 files — 含 .json/.mca/.conf/.png/.txt/.pptx）

注意：`@Transactional` 确保测试后数据回滚，不会污染数据库。

## 六、给未来 Claude 的备忘录

1. 添加测试先看这个文档，避免重复
2. `AuthServiceTest` 用了 package-private 的 `getCaptchaCodeForTest()` — 这是有意暴露的测试辅助方法，不要删
3. 需要覆盖 Sa-Token 的测试（登录成功/注册成功）请用 MockMvc，不要直接调 Service
4. `ZipExtractorTest.extract_entryAbove10MB_streamsToTempFile` 会创建 10MB+ 测试文件，跑得慢是正常的
5. `项目问题清单.md` 记录了所有安全漏洞的历史修复，改认证/存储/预览逻辑前务必先看
