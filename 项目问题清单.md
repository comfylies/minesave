# GameSaving 项目问题清单

> 生成日期：2026-06-21 | 基于全栈代码审查

---

## 目录

- [一、安全问题（🔴 高优先级）](#一安全问题-高优先级)
- [二、后端问题](#二后端问题)
- [三、前端问题](#三前端问题)
- [四、数据库问题](#四数据库问题)
- [五、测试问题](#五测试问题)
- [六、代码质量问题](#六代码质量问题)
- [七、配置与部署问题](#七配置与部署问题)

---

## 一、安全问题（🔴 高优先级）

### 1.1 认证缺失 — 大部分写操作端点无保护

**严重程度：🔴 严重**

SaTokenConfig 的路由拦截器将 `/api/articles/**`、`/api/games/**` 整体设为公开，导致所有增删改操作无需登录即可执行。

| 端点 | 方法 | 当前状态 | 应有状态 |
|------|------|----------|----------|
| `POST /api/articles` | 创建文章 | 公开 | 需登录 |
| `PUT /api/articles/{id}` | 编辑文章 | 公开 | 需登录 + 所有权检查 |
| `DELETE /api/articles/{id}` | 删除文章 | 公开 | 需登录 + 所有权检查 |
| `POST /api/games` | 创建游戏 | 公开 | 需管理员 |
| `PUT /api/games/{id}` | 编辑游戏 | 公开 | 需管理员 |
| `DELETE /api/games/{id}` | 删除游戏 | 公开 | 需管理员 |
| `PUT /api/users/{id}` | 更新用户资料 | 公开 | 需登录 + 所有权检查 |

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/config/SaTokenConfig.java`（第32-36行）
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/controller/ArticleController.java`
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/controller/GameController.java`
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/controller/UserController.java`

**修复建议：**
```java
// SaTokenConfig.java — 改为仅开放读操作
.match("/api/articles/**", r -> {
    if ("GET".equals(r.getMethod())) {
        r.stop();  // 读操作公开
    }
    // 写操作走默认的 checkLogin()
})
```

---

### 1.2 客户端控制 userId — 身份冒充漏洞

**严重程度：🔴 严重**

`ArticleCreateRequest` 和 `CommentCreateRequest` 中包含 `userId` 字段并由客户端提交，攻击者可伪造任意用户的身份创建文章和评论。

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/model/dto/ArticleCreateRequest.java`（userId 字段）
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/model/dto/CommentCreateRequest.java`（userId 字段）
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/service/impl/ArticleServiceImpl.java`（第80行，直接使用 `request.getUserId()`）
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/service/impl/CommentServiceImpl.java`（直接使用 `request.getUserId()`）

**修复建议：**
```java
// 从当前登录会话获取 userId，移除 DTO 中的 userId 字段
Long userId = StpUtil.getLoginIdAsLong();
// 或者如果确实需要指定作者（如管理员代发），加权限检查
```

---

### 1.3 敏感信息泄露（PII）

**严重程度：🔴 严重**

- `GET /api/users` — 返回所有用户的手机号、邮箱，无分页，无认证要求
- `GET /api/users/{id}` — 同样返回未脱敏的 PII，无认证要求

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/controller/UserController.java`（第37-53行）
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/service/impl/UserServiceImpl.java`（第146-149行 `getAllUsers()`）

**修复建议：**
```java
// 方案1：脱敏返回
user.setPhone(maskPhone(user.getPhone()));  // 138****1234
user.setEmail(maskEmail(user.getEmail()));  // a***@example.com

// 方案2：仅本人和管理员可查看完整信息
if (!isSelf(userId) && !isAdmin()) {
    user.setPhone(null);
    user.setEmail(null);
}
```

---

### 1.4 CORS 配置过于宽松

**严重程度：🟡 中等**

`allowedOriginPatterns("*")` + `allowCredentials(true)` 的组合允许任意域携带凭据请求，在生产环境是安全漏洞。

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/config/CorsConfig.java`

**修复建议：**
```java
// 开发环境
@Profile("dev")
configuration.addAllowedOriginPattern("*");
// 生产环境
@Profile("prod")
configuration.addAllowedOrigin("https://your-frontend-domain.com");
```

---

### 1.5 XSS 过滤不完整

**严重程度：🟡 中等**

自写的正则 XSS 过滤器无法防御混淆攻击（如 `on\0load`、`ONLOAD`、编码绕过等）。

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/util/XssFilter.java`

**修复建议：**
引入 OWASP Java HTML Sanitizer：
```xml
<dependency>
    <groupId>com.googlecode.owasp-java-html-sanitizer</groupId>
    <artifactId>owasp-java-html-sanitizer</artifactId>
</dependency>
```

---

### 1.6 application.yaml 明文密钥

**严重程度：🟡 中等（个人项目可接受）**

数据库密码和邮箱授权码硬编码在配置文件中。

**涉及文件：**
- `Backend/GameSaves/src/main/resources/application.yaml`（第11行、第40-41行）

**修复建议：**
```yaml
# 改为环境变量引用
spring:
  datasource:
    password: ${DB_PASSWORD}
  mail:
    password: ${MAIL_PASSWORD}
```

---

## 二、后端问题

### 2.1 认证路由重复 — AuthController vs UserController

**严重程度：🟡 中等**

`UserController` 中存在 `/api/users/login` 和 `/api/users/register` 端点，与 `AuthController` 功能重复，且 UserController 的注册没有验证码保护，形成安全后门。

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/controller/UserController.java`
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/controller/AuthController.java`

**修复建议：**
删除 UserController 中的 login/register 端点，统一使用 AuthController。

---

### 2.2 AuthController 直接注入 Repository

**严重程度：🟢 轻微**

AuthController 直接注入 `UserRepository` 进行登录检查（第26行），绕过了 Service 层。违反分层架构原则。

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/controller/AuthController.java`

**修复建议：**
将直接查询逻辑下沉到 `AuthService`。

---

### 2.3 ZipExtractionService.extract() 方法过长

**严重程度：🟢 轻微**

`extract()` 方法约200行，包含文件解压、README处理、图片提取、状态更新等多个职责。

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/service/ZipExtractionService.java`（第79行起）

**修复建议：**
拆分为 `extractFiles()`、`extractReadme()`、`extractReadmeImages()` 等私有方法。

---

### 2.4 文件预览加载全部内容到内存

**严重程度：🟡 中等**

`FileExplorerServiceImpl` 的 `previewFile()` 使用 `Files.readAllBytes()` 将整个文件加载到内存，大文件可导致 OOM。

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/service/impl/FileExplorerServiceImpl.java`（第128行）

**修复建议：**
```java
// 限制预览大小，超过阈值的文件返回截断内容
byte[] previewBytes = Files.readAllBytes(filePath);
if (previewBytes.length > MAX_PREVIEW_SIZE) {
    previewBytes = Arrays.copyOf(previewBytes, MAX_PREVIEW_SIZE);
    truncated = true;
}
```

---

### 2.5 ZIP 完整性校验时将整个 ZIP 读入内存

**严重程度：🟡 中等**

`ZipExtractor` 的 `computeSha256()` 使用 `Files.readAllBytes()` 加载整个 ZIP 文件用于 SHA-256 计算，200MB 的 ZIP 会导致约 400MB 堆内存占用。

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/util/ZipExtractor.java`（第50行）

**修复建议：**
```java
// 使用流式摘要
try (InputStream is = Files.newInputStream(zipPath)) {
    byte[] buffer = new byte[8192];
    int read;
    while ((read = is.read(buffer)) != -1) {
        digest.update(buffer, 0, read);
    }
}
```

---

### 2.6 PathTraversalValidator 过度拒绝

**严重程度：🟢 轻微**

拒绝所有包含 `..` 的路径，导致合法文件名如 `test..txt` 被误判。不过这是安全与功能之间的合理折中。

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/util/PathTraversalValidator.java`（第28行）

---

### 2.7 MarkdownRenderer 不支持表格

**严重程度：🟢 轻微**

自写正则渲染器不支持 Markdown 表格语法，README 文档中常见的表格无法渲染。

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/util/MarkdownRenderer.java`

**修复建议：**
引入 commonmark-java 库替换自写渲染器（后端），与前端使用的 `marked` 库保持一致。

---

### 2.8 验证码缓存无过期清理

**严重程度：🟢 轻微**

`captchaCache` 只在使用时清理对应 key，从未被使用的验证码（用户打开登录页后离开）永不过期，仅靠 1000 条上限被动删最旧条目。

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/service/AuthService.java`

**修复建议：**
使用 Guava Cache 或 Caffeine 并设置 TTL：
```java
private final Cache<String, String> captchaCache = Caffeine.newBuilder()
    .maximumSize(1000)
    .expireAfterWrite(120, TimeUnit.SECONDS)
    .build();
```

---

## 三、前端问题

### 3.1 工具函数重复定义

**严重程度：🟡 中等**

`formatSize`、`formatTime`、`truncate` 函数在 4 个文件中逐字复制：
- `src/views/GamePage.vue`
- `src/views/MySavesPage.vue`
- `src/views/UserProfilePage.vue`
- `src/components/ArticleMeta.vue`

**修复建议：**
创建 `src/utils/format.js`：
```javascript
export function formatSize(bytes) { /* ... */ }
export function formatTime(date) { /* ... */ }
export function truncate(str, len) { /* ... */ }
```

---

### 3.2 路由守卫仅检查前端 token 存在性

**严重程度：🟡 中等**

`router.beforeEach` 只检查 `localStorage` 中是否有 `satoken`，不验证 token 是否有效。伪造或过期的 token 可以通过守卫。

**涉及文件：**
- `Frontend/GameSaves_Fronted/src/router/index.js`（第54行起）

**修复建议：**
```javascript
// 在守卫中调用 checkAuth() 验证 token
if (to.meta.requiresAuth) {
    const authStore = useAuthStore();
    try {
        await authStore.checkAuth();  // 调 GET /api/auth/check
    } catch {
        return { name: 'Login', query: { redirect: to.fullPath } };
    }
}
```

---

### 3.3 登出时未清理其他 Store

**严重程度：🟡 中等**

登出时仅清空 `authStore`，`gameStore`、`articleStore`、`fileStore` 保留上一个用户的数据。

**涉及文件：**
- `Frontend/GameSaves_Fronted/src/stores/auth.js`

**修复建议：**
在 `logout()` 中调用其他 store 的 `$reset()`：
```javascript
async logout() {
    // ... 登出逻辑
    const gameStore = useGameStore();
    const articleStore = useArticleStore();
    const fileStore = useFileStore();
    gameStore.$reset();
    articleStore.$reset();
    fileStore.$reset();
}
```

---

### 3.4 API 请求无取消机制

**严重程度：🟢 轻微**

组件卸载或路由跳转后，发起的 API 请求继续执行，可能导致内存泄漏或对已卸载组件的状态更新。

**涉及文件：**
- `Frontend/GameSaves_Fronted/src/api/` 下所有模块

**修复建议：**
使用 `AbortController`：
```javascript
const controller = new AbortController();
onBeforeUnmount(() => controller.abort());
// 请求中传入 signal: controller.signal
```

---

### 3.5 bcryptjs 依赖未使用

**严重程度：🟢 轻微**

`package.json` 中 `bcryptjs` 是 Node.js 服务端库，在前端浏览器代码中无法使用且未被引用。

**涉及文件：**
- `Frontend/GameSaves_Fronted/package.json`

**修复建议：**
删除该依赖。

---

### 3.6 无 TypeScript

**严重程度：🟢 轻微**

整个前端使用纯 JavaScript，无类型安全。API 响应、Store state、Props/Events 均为隐式类型。

**涉及范围：** 全部 42 个源文件

**修复建议：**
逐步迁移为 TypeScript（可从 stores 和 api 层开始）。

---

### 3.7 图标导入方式不一致

**严重程度：🟢 轻微**

部分视图从 `@element-plus/icons-vue` 按需导入图标（如 `Search`、`User`），部分依赖 `main.js` 中的全局注册。两套方案混用。

**修复建议：**
统一为全局注册或按需导入一种方式。

---

### 3.8 Admin 视图硬编码颜色值

**严重程度：🟢 轻微**

`AdminDashboard.vue`、`AdminUsers.vue`、`AdminArticles.vue` 中使用 `#303133`、`#909399` 等硬编码颜色，未使用 CSS 变量，与全局主题系统脱节。

---

## 四、数据库问题

### 4.1 savings.article_id 缺少 UNIQUE 约束

**严重程度：🟡 中等**

任务书 ER 图标注 `article 1--1 savings`（一对一），但 schema 中 `savings.article_id` 无 UNIQUE 约束，允许一个 article 对应多个 savings 记录。

**涉及文件：**
- `gamesaving_database.sql`（savings 表定义）

**修复建议：**
```sql
-- 如果确实是 1-1 关系
ALTER TABLE savings ADD UNIQUE KEY uk_article_id (article_id);
-- 如果 1-N（为版本历史预留），则修正 ER 图
```

---

### 4.2 缺少 CHECK 约束

**严重程度：🟡 中等**

- `article.status` 无数据库层面约束，可插入 `'UPLODING'` 等拼写错误
- `users.role` 无约束，可插入任意角色值

**涉及文件：**
- `gamesaving_database.sql`

**修复建议：**
```sql
ALTER TABLE article ADD CONSTRAINT chk_article_status
    CHECK (status IN ('UPLOADING', 'EXTRACTING', 'READY', 'FAILED'));
ALTER TABLE users ADD CONSTRAINT chk_users_role
    CHECK (role IN ('admin', 'user'));
-- 或使用 ENUM 类型
```

---

### 4.3 idx_game_id 索引冗余

**严重程度：🟢 轻微**

`article` 表上的单列索引 `idx_game_id(game_id)` 被两个复合索引的最左前缀覆盖：
- `idx_game_status_created(game_id, status, created_at)`
- `idx_game_user(game_id, user_id)`

该索引仅增加维护开销，无查询收益。

**涉及文件：**
- `gamesaving_database.sql`（article 表索引）

**修复建议：**
```sql
DROP INDEX idx_game_id ON article;
```

---

### 4.4 VARCHAR(500) 对于 URL/路径字段可能不够

**严重程度：🟢 轻微**

`avatar_url`、`cover_url`、`storage_root`、`zip_path`、`extract_root` 使用 `VARCHAR(500)`。带 CDN 参数的 URL 和深层嵌套路径可能超过此限制。

**修复建议：**
将 URL 类字段扩展为 `VARCHAR(2048)`，路径类字段扩展为 `VARCHAR(1000)`。

---

### 4.5 error_message VARCHAR(1000) 偏短

**严重程度：🟢 轻微**

异常消息+截断的堆栈信息可能超出 1000 字符。

**涉及文件：**
- `gamesaving_database.sql`（article 表）

**修复建议：**
```sql
ALTER TABLE article MODIFY COLUMN error_message TEXT;
```

---

### 4.6 测试数据覆盖不足

**严重程度：🟡 中等**

| 缺失场景 | 影响 |
|----------|------|
| 无 FAILED 状态文章 | 错误处理 UI 无法测试 |
| 无 UPLOADING/EXTRACTING 状态 | 状态转换流程无法测试 |
| 无评论回复（parent_id） | 嵌套评论功能无法测试 |
| 无 download_logs 数据 | 下载限流和审计无法测试 |
| 无禁用用户（is_active=0） | 账户冻结场景无法测试 |
| comment.quote_start 和 quote_end 均为 0 | 批注定位无法验证 |
| 无空快照（file_count=0） | 边界场景未覆盖 |

**涉及文件：**
- `test_data_insert.sql`

---

## 五、测试问题

### 5.1 后端测试几乎为零

**严重程度：🔴 严重**

仅存在一个 `contextLoads()` Spring 上下文冒烟测试，无任何单元测试、集成测试或 API 测试。

**涉及文件：**
- `Backend/GameSaves/src/test/java/com/gamesaves/gamesaves/GameSavesApplicationTests.java`

**建议优先补充的测试：**

| 优先级 | 测试目标 | 测试类型 |
|--------|---------|---------|
| 1 | `AuthService.loginWithPassword()` — 正常/错误密码/锁定 | 单元测试 |
| 2 | `PathTraversalValidator` — 拒绝 `../`/绝对路径/盘符 | 单元测试 |
| 3 | `CommentServiceImpl.deleteComment()` — 作者删除/管理员删除/他人删除 | 单元测试 |
| 4 | `ArticleController` CRUD — 认证/授权 | 集成测试 |
| 5 | `ZipExtractor` — 正常 ZIP/空 ZIP/恶意路径 ZIP | 单元测试 |

---

### 5.2 前端测试完全缺失

**严重程度：🔴 严重**

无 Vitest/Jest/Cypress 等任何测试框架，`package.json` 的 scripts 中无 test 命令。

**涉及文件：**
- `Frontend/GameSaves_Fronted/package.json`

**建议：**
```bash
npm install -D vitest @vue/test-utils jsdom
# 先从 Store 和 composable 的单元测试开始
```

---

## 六、代码质量问题

### 6.1 GameServiceImpl.deleteGame() 依赖数据库报错

**严重程度：🟢 轻微**

删除游戏时不做前置检查，依赖 `DataIntegrityViolationException` 来阻止删除有关联文章的游戏。虽然能工作，但非最佳实践。

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/service/impl/GameServiceImpl.java`（第99行）

**修复建议：**
```java
// 先检查是否有文章
long articleCount = articleRepository.countByGameId(gameId);
if (articleCount > 0) {
    throw new BadRequestException("无法删除，该游戏下存在 " + articleCount + " 篇文章");
}
```

---

### 6.2 CommentServiceImpl 中的 "force load"

**严重程度：🟢 轻微**

第79行 `commentRepository.findById(comment.getId())` 看起来是为了触发 JPA 懒加载的 workaround，暗示实体关系配置可能有问题。

**涉及文件：**
- `Backend/GameSaves/src/main/java/com/gamesaves/gamesaves/service/impl/CommentServiceImpl.java`

---

### 6.3 Lombok @Data 在 JPA 实体上

**严重程度：🟢 轻微**

`@Data` 生成的 `equals()` 和 `hashCode()` 包含懒加载字段，可能在特定场景下导致 `LazyInitializationException` 或集合比较问题。

**涉及范围：** 所有实体类

**修复建议：**
```java
// 替换 @Data 为
@Getter
@Setter
@ToString
// 并手动实现 equals/hashCode 基于 ID
```

---

## 七、配置与部署问题

### 7.1 storage-path 使用相对路径

**严重程度：🟡 中等**

`app.storage.database-path: ../../Database` 是相对于工作目录的相对路径，启动目录不同会导致路径解析错误。

**涉及文件：**
- `Backend/GameSaves/src/main/resources/application.yaml`（第74行）

**修复建议：**
```yaml
app:
  storage:
    database-path: ${STORAGE_PATH:../../Database}  # 支持环境变量覆盖
```
或使用绝对路径。

---

### 7.2 无数据库迁移工具

**严重程度：🟢 轻微**

使用 `ddl-auto: validate` 但无 Flyway/Liquibase 管理 schema 变更。多人协作或多环境部署时 schema 同步靠人工。

**修复建议：**
引入 Flyway，将 `gamesaving_database.sql` 拆分为版本化迁移脚本。

---

## 问题统计

| 严重程度 | 数量 | 类别 |
|----------|------|------|
| 🔴 严重 | 7 | 安全认证(4)、测试(2)、PII泄露(1) |
| 🟡 中等 | 13 | 配置安全(2)、前后端架构(7)、数据库约束(2)、代码质量(2) |
| 🟢 轻微 | 16 | 代码规范(8)、数据库优化(3)、工具链(3)、依赖(2) |
| **合计** | **36** | |

---

## 建议修复路线图

### 第一阶段（答辩前，约 3-4 小时）
1. 修复路由拦截器，给 Article/Game/User 写操作加认证
2. 将 userId 从请求体改为从 Session 获取
3. User API 添加 PII 脱敏
4. 补充 3-5 个关键测试

### 第二阶段（提升完成度，约 5-8 小时）
5. 删除 UserController 重复的登录/注册端点
6. 抽取前端重复工具函数
7. 完善测试数据
8. 添加 application-test.yaml

### 第三阶段（未来迭代，约 15-20 小时）
9. 引入 commonmark 和 OWASP HTML Sanitizer
10. 前端迁移 TypeScript
11. 引入 Flyway
12. 添加 FULLTEXT INDEX
