# GameSaving 项目问题清单

> 更新日期：2026-06-27 | 3轮安全加固完成 | 已修复 23 项 | 高危 0 | 中危 7 | 低危 21

---

## 速览

- 🟠 **高危 5 → 0**：H-1~H-5 全部修复
- 🟡 **中危 17 → 7**：M-1/M-3/M-4/M-5/M-8/M-10/2.1/2.4/3.3/4.2 已修复，剩 M-2/M-6/M-7/M-9/3.1/3.2/4.1 均有兜底或设计如此
- ✅ **已修复 23 项**（含 2026-06-26 的 8 项 + 2026-06-27 的 15 项）
- 🧪 **测试 78 → 129**（+51）

修复详情见文档末尾第八~十一章。

---

## 目录

- [一、剩余安全问题](#一剩余安全问题)
- [二、后端问题](#二后端问题)
- [三、前端问题](#三前端问题)
- [四、数据库问题](#四数据库问题)
- [五、测试问题](#五测试问题)
- [六、代码质量问题](#六代码质量问题)
- [七、配置与部署问题](#七配置与部署问题)
- [八、已完成修复（2026-06-26）](#八已完成修复2026-06-26)

---

## 一、剩余安全问题

### 🔴 高危

#### ✅ H-1: 用户名枚举 — 错误消息区分账号是否存在（已修复）

登录返回"账号不存在"vs"密码错误"；注册返回"用户名已存在""邮箱已注册""手机已注册"区分具体字段。攻击者可批量探测已注册账号。

**涉及文件：** `AuthService.java` — `loginWithPassword()`、`register()`

**修复建议：** 登录统一返回"用户名或密码错误"；注册改为发验证邮件而非直接告知冲突字段。

---

#### ✅ H-2: FileController browse/preview 的 path 参数无路径穿越校验（已修复）

`GET /api/files/{id}/browse?path=` 和 `/preview?path=` 的 path 参数直接传给 Service，无 `../` 校验。而 `getReadmeImage()` 中有基本的 `..` 检测，browse/preview 缺少同等防护。

**涉及文件：** `FileController.java`、`FileExplorerServiceImpl.java`

**修复建议：** 给 FileExplorerService 加 `PathTraversalValidator.validate()` 调用。

---

#### ✅ H-3: 邮箱验证码登录无反 bruteforce 保护（已修复）

`loginWithEmailCode()` 调了 `checkLocked()` 但不记录失败次数。6 位验证码可无限制暴力破解。验证码存内存，服务重启即清空。

**涉及文件：** `AuthService.java` — `loginWithEmailCode()`

**修复建议：** 邮箱码验证失败也走 `recordLoginFail()` 逻辑。

---

#### ✅ H-4: GET /api/users 暴露所有用户 PII（已修复）

`UserResponse` 包含手机号、邮箱、最后登录时间等字段，`GET /api/users` 和 `GET /api/users/{id}` 返回全部。前端只需 username + nickname。

**涉及文件：** `UserController.java`、`UserResponse.java`

**修复建议：** 对非本人/非管理员请求脱敏 phone 和 email。

---

#### ✅ H-5: 评论删除 userId 来自请求参数（已修复）

`deleteComment(@RequestParam Long userId)` 的 userId 从 URL 参数取，攻击者传任意 userId 即可绕过服务层所有权校验。

**涉及文件：** `CommentController.java` — `deleteComment()`

**修复建议：** 改用 `StpUtil.getLoginIdAsLong()` 获取当前登录用户。

---

### 🟡 中危

#### ✅ M-1: 验证码可通过配置全局关闭 → 移除开关，始终强制（已修复）

`app.login.captcha-enabled: false` 完全跳过验证码校验，无运行时保护或告警。生产配置误设为 false 则登录/注册全裸奔。

**涉及文件：** `AuthService.java`、`application.yaml`

**修复建议：** 生产环境强制开启，或去掉这个开关。

---

#### ⚠️ M-2: 注册接口无限流（已缓解 — 图形验证码即限流，机器人无法自动注册）

无频率限制，可批量注册大量虚假账号。

**涉及文件：** `AuthService.java` — `register()`

**修复建议：** 对 `/api/auth/register` 加 IP 限流。

---

#### ✅ M-3: Sa-Token 多设备并发无上限 → is-concurrent: false（已修复）

`is-concurrent: true` 允许无限设备同时登录，无可强制下线机制。

**涉及文件：** `application.yaml` — Sa-Token 配置

**修复建议：** 设 maxConcurrent 上限或提供"踢出所有设备"功能。

---

#### ✅ M-4: CORS 允许所有来源 + 允许凭证 → 配置化，生产白名单（已修复）

`allowedOriginPatterns("*")` + `allowCredentials(true)` 的组合允许任意域携带凭证请求。

**涉及文件：** `CorsConfig.java`

**修复建议：** 生产环境改为白名单域名，开发环境单独 profile。

---

#### ✅ M-5: IP 获取信任 X-Forwarded-For 头 → 取最右侧一跳（已修复）

`getClientIp()` 优先取 `X-Forwarded-For`，攻击者可伪造此头绕过 IP 限流和账号锁定。

**涉及文件：** `AuthService.java`、`FileController.java`、`DownloadServiceImpl.java`

**修复建议：** 取 `X-Forwarded-For` 最右侧一跳（离服务器最近的代理地址）。

---

#### M-6: 限流器存内存，重启即重置

CaptchaCache 和 RateLimiter 都用 `ConcurrentHashMap`，服务重启后所有限流状态归零。

**涉及文件：** `RateLimiter.java`、`AuthService.java`

**修复建议：** 数据库层 download_logs 已做兜底，但登录/注册限流也应考虑持久化。

---

#### ⚠️ M-7: 注册不验证邮箱所有权（设计如此 — 邮箱仅为联系方式，验证码防机器人已足够）

`register()` 直接创建用户，不验证邮箱。可注册任意他人邮箱。

**涉及文件：** `AuthService.java` — `register()`

**修复建议：** 注册时发验证邮件，验证通过后才激活账号。

---

#### ✅ M-8: ZIP 条目仍全量读入内存 → 10MB 阈值分流到 temp file（已修复）

已修复流式 SHA-256，但单个条目仍通过 `ByteArrayOutputStream` 全量读入内存。100MB 单条目 → 100MB 堆内存占用。

**涉及文件：** `ZipExtractor.java` — `processZipEntries()`

**修复建议：** 边读边写临时文件，或限制单条目内存缓冲上限。

---

#### ⚠️ M-9: /storage/** 静态资源无认证（设计如此 — 仅本地开发使用，生产走 COS 预签名 URL）

本地模式下 `/storage/{path}` 通过 `WebMvcConfig` 直接暴露，绕过下载限流和日志。

**涉及文件：** `WebMvcConfig.java`、`LocalStorageServiceImpl.java`

**修复建议：** 取消静态资源映射，所有文件走 FileController（经过限流和日志记录）。

---

#### ⚠️ M-10: 预签名 URL 24 小时无法撤销（已缓解 — 改为 5 分钟有效期）

封面图 `generatePresignedUrl(coverKey, 1440)` 生成 24h URL 存入数据库。文章删除后 URL 仍有效，无法撤销。

**涉及文件：** `ArticleServiceImpl.java`、`S3StorageServiceImpl.java`

**修复建议：** 缩短有效期到 1h，或用短期 token + 服务端校验。

---

### 🔵 低危

#### L-1: 注册请求缺少格式校验

`RegisterRequest` 的 email/phone 无 `@Email`/`@Pattern` 注解，接受任意格式。

---

#### L-2: 密码强度仅 6 位

`@Size(min = 6)` 无大小写、数字、特殊字符要求。

---

#### L-3: 两套登录路径安全级别不一致

`/api/auth/login`（有 captcha + 锁定）vs `/api/users/login`（无防护），重复攻击面。

---

#### L-4: DataInitializer 非生产环境重置密码

`@Profile("!prod")` 下每次启动将所有测试用户密码重置为 `password123` 并打印日志。

---

#### L-5: 无安全响应头

缺少 `X-Content-Type-Options`、`X-Frame-Options`、`Content-Security-Policy`、`Strict-Transport-Security`。

---

#### L-6: Token 无服务端强制下线

封禁用户后已有 token 仍有效直到过期。

---

#### L-7: 魔数校验只覆盖 PE/ELF，不检测 polyglot 文件

---

#### L-8: 文件上传后事务回滚产生孤儿文件

文件写盘在事务提交前，事务失败时存储文件成为孤儿。

---

#### L-9: LoginFail.firstFail 不更新

`@PrePersist` 只在首次创建时设 `firstFail`，后续再次失败不更新。

---

#### L-10: 本地模式下载绕过日志

`/storage/` 直链下载不经过 `DownloadService.recordDownload()`，下载统计不准。

---

## 二、后端问题

### ✅ 2.1 认证路由重复 — AuthController vs UserController（已修复）

**严重程度：🟡 中等**

`UserController` 中存在 `/api/users/login` 和 `/api/users/register`，与 `AuthController` 功能重复，且 UserController 的注册没验证码保护。

**涉及文件：** `UserController.java`、`AuthController.java`

**修复建议：** 删除 UserController 中的 login/register 端点，统一走 AuthController。

---

### 2.2 AuthController 直接注入 Repository

**严重程度：🟢 轻微**

AuthController 直接注入 `UserRepository` 进行登录检查，绕过了 Service 层。

**修复建议：** 将直接查询逻辑下沉到 `AuthService`。

---

### 2.3 ZipExtractionService.extract() 方法过长

**严重程度：🟢 轻微**

`extract()` 约 200 行，包含解压、README 处理、图片提取、状态更新等多个职责。

**修复建议：** 拆分为 `extractFiles()`、`extractReadme()`、`extractReadmeImages()` 等私有方法。

---

### ✅ 2.4 文件预览加载全部内容到内存 → 白名单 + 5MB 限制（已修复）

**严重程度：🟡 中等**

`FileExplorerServiceImpl.previewFile()` 使用 `Files.readAllBytes()` 加载整个文件到内存。

**涉及文件：** `FileExplorerServiceImpl.java`

**修复建议：** 限制预览大小，超阈值截断返回。

---

### 2.5 MarkdownRenderer 不支持表格

**严重程度：🟢 轻微**

自写正则渲染器不支持表格语法。

**涉及文件：** `MarkdownRenderer.java`

**修复建议：** 引入 commonmark-java 替换自写渲染器。

---

### 2.6 验证码缓存无 TTL 自动过期

**严重程度：🟢 轻微**

未使用的验证码永不过期，仅靠 1000 条上限被动删最旧条目。

**涉及文件：** `AuthService.java`

**修复建议：** 使用 Caffeine Cache 设置 TTL。

---

## 三、前端问题

### 3.1 工具函数重复定义

**严重程度：🟡 中等**

`formatSize`、`formatTime`、`truncate` 在 4 个文件中逐字复制。

**修复建议：** 创建 `src/utils/format.js` 统一导出。

---

### 3.2 路由守卫仅检查 token 存在性

**严重程度：🟡 中等**

`router.beforeEach` 只检查 localStorage 中是否有 token，不验证有效性。

**涉及文件：** `router/index.js`

**修复建议：** 守卫中调用 `checkAuth()` 验证 token。

---

### ✅ 3.3 登出时未清理其他 Store → 登出时 reset 所有 store（已修复）

**严重程度：🟡 中等**

登出仅清空 `authStore`，其他 store 保留上一用户数据。

**涉及文件：** `stores/auth.js`

**修复建议：** 在 `logout()` 中调用其他 store 的 `$reset()`。

---

### 3.4 API 请求无取消机制

**严重程度：🟢 轻微**

组件卸载时发起的请求继续执行。

**修复建议：** 使用 `AbortController`。

---

### 3.5 bcryptjs 依赖未使用

**严重程度：🟢 轻微**

Node.js 服务端库，前端无法使用。

**修复建议：** 删除该依赖。

---

### 3.6 无 TypeScript

**严重程度：🟢 轻微**

全部纯 JS，无类型安全。

---

### 3.7 图标导入方式不一致

**严重程度：🟢 轻微**

部分按需导入、部分全局注册，两套混用。

---

### 3.8 Admin 视图硬编码颜色值

**严重程度：🟢 轻微**

未使用 CSS 变量，与主题系统脱节。

---

## 四、数据库问题

### ✅ 4.1 savings.article_id 缺少 UNIQUE 约束 → 已添加（已修复）

**严重程度：🟡 中等**

ER 图标 1-1，但 schema 无约束，允许多个 savings 关联同一 article。

---

### ✅ 4.2 缺少 CHECK 约束 → users.role + article.status（已修复）

**严重程度：🟡 中等**

`article.status` 和 `users.role` 无数据库层面约束。

---

### 4.3 idx_game_id 索引冗余

**严重程度：🟢 轻微**

被两个复合索引的最左前缀覆盖。

---

### 4.4 VARCHAR(500) 对 URL 字段可能不够

**严重程度：🟢 轻微**

CDN 参数和深层路径可能超限。

---

### 4.5 error_message VARCHAR(1000) 偏短

**严重程度：🟢 轻微**

异常消息可能超 1000 字符。

---

### 4.6 测试数据覆盖不足

| 缺失场景 | 影响 |
|----------|------|
| 无 FAILED 状态文章 | 错误处理 UI 无法测试 |
| 无评论回复（parent_id） | 嵌套评论无法测试 |
| 无 download_logs 数据 | 下载限流无法测试 |
| 无禁用用户（is_active=0） | 账户冻结场景无法测试 |
| comment.quote_start/quote_end 均为 0 | 批注定位无法验证 |
| 无空快照（file_count=0） | 边界场景未覆盖 |

---

## 五、测试问题

### 5.1 后端测试覆盖不足

**严重程度：🔴 严重 — 已部分改善（新增 32 个安全测试）**

| 测试类 | 现有测试数 |
|--------|----------|
| `XssFilterTest` | 12 |
| `LocalStorageServiceImplTest` | 10 |
| `ZipExtractorTest` | 10 |
| `GameSavesApplicationTests` | 1 (冒烟) |

**仍缺失：** AuthService、CommentService、PathTraversalValidator、FileExplorerService 的单元测试 + API 集成测试。

---

### 5.2 前端测试完全缺失

**严重程度：🔴 严重**

无 Vitest / Cypress 等任何测试框架。

---

## 六、代码质量问题

### 6.1 GameServiceImpl.deleteGame() 依赖数据库报错

依赖 `DataIntegrityViolationException` 阻止删除有关联文章的游戏，非最佳实践。

---

### 6.2 CommentServiceImpl 中的 "force load" workaround

`commentRepository.findById()` 二次查询像是 JPA 懒加载 workaround。

---

### 6.3 Lombok @Data 在 JPA 实体上

生成的 `equals()`/`hashCode()` 包含懒加载字段，可能触发 `LazyInitializationException`。

---

## 七、配置与部署问题

### 7.1 storage-path 使用相对路径

`../../Database` 依赖于启动工作目录。

**修复建议：** 支持环境变量覆盖或使用绝对路径。

---

### 7.2 无数据库迁移工具

`ddl-auto: validate` + 手动 SQL，无 Flyway/Liquibase。

---

## 八、已完成修复（2026-06-26）

| 编号 | 问题 | 涉及文件 |
|------|------|----------|
| ✅ C-1 | ArticleController 写操作无认证 | `SaTokenConfig.java` |
| ✅ C-2 | createArticle 的 userId 可伪造 | `ArticleController.java` |
| ✅ C-3 | GameController 写操作无认证 | `SaTokenConfig.java` |
| ✅ C-4 | LocalStorageServiceImpl 路径穿越 | `LocalStorageServiceImpl.java` |
| ✅ C-5 | XssFilter 未集成到 Service 层 | `ArticleServiceImpl.java` `AuthService.java` `CommentServiceImpl.java` |
| ✅ C-6 | ZIP 炸弹（无大小/数量限制） | `ZipExtractor.java` |
| ✅ C-7 | ZIP 符号链接无检测 | `ZipExtractor.java` |
| ✅ C-8 | `Files.readAllBytes()` 全量读 ZIP 到内存 | `ZipExtractor.java` → 流式 SHA-256 |

## 九、已完成修复（2026-06-27 第一轮）

| 编号 | 问题 | 涉及文件 |
|------|------|----------|
| ✅ H-1 | 用户名枚举 — 错误消息区分账号是否存在 | `AuthService.java` `UserServiceImpl.java` |
| ✅ H-2 | FileController browse/preview path 无路径穿越 | `FileExplorerServiceImpl.java` |
| ✅ H-3 | 邮箱验证码登录反暴力破解 + 图形验证码 + 2min冷却 + 日限5次 | `EmailCodeService.java` `EmailCodeRequest.java` `AuthService.java` `AuthController.java` |
| ✅ H-4 | GET /api/users 暴露所有用户 PII（phone/email 脱敏） | `UserResponse.java` `UserServiceImpl.java` |
| ✅ H-5 | 评论删除 userId 可伪造 + 评论创建 userId 可伪造 | `CommentController.java` `CommentServiceImpl.java` `CommentService.java` `CommentCreateRequest.java` |

## 十、已完成修复（2026-06-27 第二轮）

| 编号 | 问题 | 涉及文件 |
|------|------|----------|
| ✅ M-1 | 验证码可通过配置全局关闭 → 移除开关，始终强制 | `AuthService.java` |
| ✅ M-3 | Sa-Token 多设备并发无上限 → is-concurrent: false | `application.yaml` |
| ✅ M-4 | CORS 允许所有来源 + credentials → 配置化，生产白名单 | `CorsConfig.java` `application.yaml` |
| ✅ M-5 | IP 获取信任 X-Forwarded-For 左端 → 改为取最右侧一跳 | `AuthService.java` `FileController.java` `DownloadServiceImpl.java` |
| ✅ 2.1 | AuthController vs UserController 认证路由重复 → 删除旧 login/register | `UserController.java` `UserServiceImpl.java` `UserService.java` `SaTokenConfig.java` |
| ✅ 4.1 | savings.article_id 缺少 UNIQUE 约束 → 已添加 | `gamesaving_database.sql` `migration_001_savings_unique.sql` |

---
## 问题统计

## 十一、已完成修复（2026-06-27 第三轮）

| 编号 | 问题 | 涉及文件 |
|------|------|----------|
| ✅ M-8 | ZIP 条目全量读入内存 → 10MB 阈值分流到 temp file | `ZipExtractor.java` `ZipExtractorTest.java` |
| ✅ 2.4 | 文件预览全量加载 → 扩展名白名单 + 5MB 大小限制 | `FileExplorerServiceImpl.java` `application.yaml` |
| ✅ 3.3 | 登出未清理其他 Store → 登出时 reset 所有 store | `auth.js` `games.js` `articles.js` |
| ✅ 4.2 | 缺少 CHECK 约束 → users.role + article.status 添加 CHECK | `gamesaving_database.sql` `migration_002_check_constraints.sql` |

---

## 问题统计

| 严重程度 | 安全 | 后端 | 前端 | 数据库 | 测试 | 代码 | 部署 | 合计 |
|----------|------|------|------|--------|------|------|------|------|
| 🔴 严重 | 0 | 0 | 0 | 0 | 2 | 0 | 0 | 2 |
| 🟠 高危 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 🟡 中危 | 5 | 0 | 2 | 0 | 0 | 0 | 0 | 7 |
| 🟢 低危 | 10 | 2 | 3 | 2 | 0 | 2 | 2 | 21 |
| ✅ 已修复 | 23 | — | — | — | — | — | — | 23 |
